
import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    root: {
        minWidth: 375,
        minHeight: 600,
        margin: '30px 30px 0px 30px',
        textAlign: 'center',
    },
    title: {
        fontSize: 14,
        marginBottom: 20,
    },
    pos: {
        marginBottom: 20,
    },
});
export { useStyles }